# Coupled RAFT and DEQ-Flow

This example solves the coupled hidden/flow state, supervises selected solver
states with fixed-point correction, and reuses the resulting state.

```python
--8<-- "examples/raft_deq_flow.py"
```
