## Extend Beyond the Quick Reference

The signatures above define the public entry points. A custom transition must
also preserve the equilibrium-state shape, device, dtype, finite values, and
state gradients. The [extension tutorial](../../learn/extending-silva.md)
derives that contract, while the
[reproduction protocol](../../learn/reproducing-silva-and-papers.md) separates
compact mechanism checks from source-scale benchmark claims.
